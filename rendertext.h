#ifndef RENDER_TEXT_H
#define RENDER_TEXT_H

#include <string>

// Function declaration for rendering text
void renderText(float x, float y, const std::string& text, void* font);

#endif // RENDER_TEXT_H
